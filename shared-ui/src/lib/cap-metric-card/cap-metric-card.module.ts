import { NgModule } from '@angular/core';
import { CapMetricCardComponent } from './cap-metric-card.component';

@NgModule({
  imports: [CapMetricCardComponent],
  exports: [CapMetricCardComponent],
})
export class CapMetricCardModule {}
